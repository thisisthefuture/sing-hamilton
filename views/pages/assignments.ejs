
		<h2><%= song %></h2>

		<table>
			<% assignments.forEach(function(el, index) { %>
			<tr>
				<td><%= el.name %>: </td>
				<td><% el.role = el.role.join().replace(/\,/g,', '); %> <%= el.role %> </td>
			</tr>

			<% }); %>
		</table>
